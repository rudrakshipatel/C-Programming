#include <stdio.h>

int main() {
    int n, sum;
    printf("Enter value of n: ");
    scanf("%d", &n);

    sum = n * n;  // sum of first n odd numbers

    printf("Sum of first %d odd numbers = %d\n", n, sum);
    return 0;
}
