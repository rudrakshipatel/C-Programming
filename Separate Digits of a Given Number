#include <stdio.h>

int main() {
    int num, rem;
    printf("Enter a number: ");
    scanf("%d", &num);

    printf("Digits (from last to first):\n");
    while(num > 0) {
        rem = num % 10;
        printf("%d ", rem);
        num /= 10;
    }

    return 0;
}
