#include <stdio.h>

int main() {
    int n, sum;
    printf("Enter value of n: ");
    scanf("%d", &n);

    sum = n * (n + 1);  // sum of first n even numbers

    printf("Sum of first %d even numbers = %d\n", n, sum);
    return 0;
}
